[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fightladder_SPAR"
description = "TODO: ADD HERE"
version = "0.1.0"

dependencies = ["numpy==1.24.4", "torch==2.4.1", "gym-retro==0.8.0", "gymnasium==1.1.1"]
requires-python = "==3.8.10"

authors = [
    { name="Justin Wang", email="jw4406@princeton.edu" },
    { name="Amit Solomon", email="as3993@princeton.edu" },
]
readme = "README.md"

[tool.setuptools]
packages = ["fightladder"]

[project.optional-dependencies]
dev = [
    "pytest",
    "pre-commit",
    "ruff",
    "ruff-lsp",
    "ipython",
    "matplotlib",
    "jupyterlab",
    "ipdb",
]